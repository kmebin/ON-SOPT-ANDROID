<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kmebin.Seminar27th">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assign3">
        <activity android:name="org.kmebin.Seminar27th.SignUpActivity"></activity>
        <activity android:name="org.kmebin.Seminar27th.MainActivity" />
        <activity android:name="org.kmebin.Seminar27th.ProfileDetailActivity" />
        <activity android:name="org.kmebin.Seminar27th.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>